package cn.toside.music.mobile;

import com.reactnativenavigation.NavigationActivity;

/**
 * The vehicle launcher owns its system bars and right-side widgets.  The car
 * edition deliberately leaves those window decisions to the host system so a
 * manufacturer overlay is not resized when this activity gains focus.
 */
public class MainActivity extends NavigationActivity {}
